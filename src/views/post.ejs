<!DOCTYPE html>
<html lang="da">
  <head>
    <%- include('./partials/head') %>
    <title>Blog Post</title>
  </head>
  <body>
    <div class="container mt-5">
      <h2><%- post.title %></h2>
      <p class="text-muted">
        <%- post.createdAt.toLocaleDateString("da-DK").split("/"); %>
      </p>

      <div class="container mb-5">
        <hr class="hr" style="width: 40%" size="6" />
      </div>

      <div class="mb-5"><%- post.content_mardown %></div>

      <div class="container" style="margin-bottom: 5rem; margin-top: 5rem">
        <img
          src="<%- post.image %>"
          alt="post image"
          class="img-fluid"
          style="max-width: 30%"
        />
      </div>

      <div class="mt-3 mb-4">
        <a href="/" class="btn btn-secondary">
          <i class="fa fa-arrow-left"></i> Tilbage
        </a>
      </div>
    </div>
  </body>
</html>
